<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('home') }}">SC - Diet & Nutri</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
      aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link" href="{{ path('home') }}">Accueil
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('recipe') }}">Recettes</a>
        </li>
        {% if app.user %}
        {% if 'ROLE_ADMIN' in app.user.roles %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path('user') }}">Utilisateurs</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('diet') }}">Régimes</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('allergen') }}">Allergènes</a>
        </li>
        {% endif %}
        {% endif %}
      </ul>
      <ul class="navbar-nav ms-auto bg-primary mx-5">
        {% if app.user %}
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true"
            aria-expanded="false">{{ app.user.firstname }} {{ app.user.lastname }}</a>
          <div class="dropdown-menu bg-secondary">
            <a class="dropdown-item" href="{{ path('user.password', {id: app.user.id}) }}">Modifier mon password</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="{{ path('logout') }}">Déconnexion</a>
          </div>
        </li>
        {% else %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path('login') }}">Connexion</a>
        </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>